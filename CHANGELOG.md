# 更新履歴

## v1.9.0 — 2026-07-10

### 追加

- **診断スコア化＋レーダーチャート**：接地・骨盤/体幹・腕振り・股関節伸展・上下動の5項目を10点満点でスコア化し、診断結果直下にplotlyレーダーチャートと総合スコア・項目別数値一覧を表示。Geminiの出力末尾（WEAKNESS_TAG行の直前）に `SCORES_JSON` 行を出力させ、`analyzer.extract_scores_json()` で抽出・検証（5キー欠落・非数値はNone扱い、値は1〜10にクランプ）。ダウンロードMarkdownにもスコア表を追加
- **タイムスタンプ引用**：「1. 全体評価（良い点）」「2. 改善すべき点」の各指摘に、動画内の該当タイムスタンプ（例：[0:15]）を添えるようプロンプトを変更

### 設定変更

- `config.py` に `SCORE_ITEMS`（5項目のキー・表示ラベル辞書）を追加
- `requirements.txt` に `plotly>=5.24.0` を追加

## v1.8.0 — 2026-07-10

### 背景

同日の実測で `gemini-3.5-flash` が高頻度で503（モデル高負荷）を返し、成功時も50秒超かかる状態を確認。加えて `google-genai` はデフォルトでリクエストタイムアウトが無く、ハング時にUIが無期限に固まる問題があった。503は1秒程度で即返るため、リトライのオーバーヘッドは小さいと判断。

### 堅牢性・UX改善

- **プログレス表示**：Step 3（フォーム解析）を別スレッド実行に変更し、経過時間ベースのプログレスバー（`ANALYZE_EXPECTED_SEC`＝120秒で95%到達、完了で100%）を表示。従来の「30秒〜2分かかります」の固定文言から、経過秒数が見えるUIに変更
- **5分タイムアウト**：`analyze_form`・`screen_video` に `types.HttpOptions(timeout=...)` を追加し、SDKデフォルトの無期限待機を解消（解析300秒／スクリーニング60秒）。タイムアウト時は `TIMEOUT_EXCEEDED` として診断回数を消費せずにエラー表示
- **503自動リトライ**：`analyze_form` が503を検知した場合、最大3回まで10秒間隔で自動リトライ。リトライ中はプログレスバーのラベルを「APIが混雑しています。自動再試行中（N回目/最大3回）...」に切り替えて表示。429（レート制限）は従来どおり即エラーでリトライしない

### 設定変更

- `config.py` に `ANALYZE_TIMEOUT_SEC`（300）・`SCREEN_TIMEOUT_SEC`（60）・`RETRY_503_MAX_ATTEMPTS`（3）・`RETRY_503_WAIT_SEC`（10）・`ANALYZE_EXPECTED_SEC`（120）を追加

---

## v1.7.0 — 2026-07-10

### 変更（UIリニューアル・機能/ロジック変更なし）

- スポーツテックHUD風にUIをリデザイン：ヒーローヘッダー（グラデーション文字タイトル＋バッジチップ「⚡ Powered by Gemini」「🔬 バイオメカニクス解析」「🆓 1日1回無料」＋グラデーション水平線）
- ステップガイド（① 動画アップロード → ② AIチェック → ③ バイオメカニクス解析）を追加
- ファイルアップローダーのドロップゾーンにシアンの点線枠＋hover時グロー効果
- プライマリボタンをシアン→ブルーのグラデーションに変更、ダウンロードボタンをシアンのアウトライン調に変更
- 診断結果見出し・記事内h2見出しにシアンのアクセントバーを追加
- フッターのリンク色・区切り線をシアン系に変更
- `.streamlit/config.toml` の `primaryColor` を `#22D3EE` に変更
- 診断結果直下のゴールドCTAカード（`render_gear_cta()`）は寒色基調との意図的コントラストとして変更なし

---

## v1.6.0 — 2026-07-10

### 追加

- 弱点連動CTA：診断結果の弱点カテゴリ（殿筋・体幹／可動域／弾性／上半身／該当なし）に応じて、診断結果直下のCTAカード（`render_gear_cta()`）の見出し・訴求文を切り替え
- Gemini（analyzer）の出力末尾に `WEAKNESS_TAG: <カテゴリ>` を1行出力させ、Python側（`analyzer.extract_weakness_tag()`）で抽出・本文から除去。カテゴリ別文言は `config.py` の `WEAKNESS_CTA_VARIANTS` で管理
- 送客先URLは当面全カテゴリ共通（リスト⑦）。カテゴリ別リストを作成後に差し替え予定

---

## v1.5.2 — 2026-07-03

### 🔧 コードレビュー対応（レポート: `docs/code-review/rfd-2026-07-03.md`）

#### フェーズ1（重大）
- 空レスポンス（`response.text` が None/空）でも1日1回の診断枠を消費するバグを修正。本文が空の場合は finish_reason 付きのエラーを表示し、カウントを進めない（R-1）
- thinkingトークンが `max_output_tokens` を消費するため、床値を 16384 → 24576 に引き上げ（AMCと同基準。空レスポンスの主要因対策）（Y-2）

#### フェーズ2（改善推奨）
- 日付処理をJST化（`config.py` に `jst_now()` ヘルパー追加）。Streamlit Cloud（UTC）で1日制限のリセットがJST 9:00になる問題、朝9時前の診断日・DLファイル名が前日になる問題を修正。cookieの `expires` も同基準に（Y-1・G-5）
- Files APIポーリング中の `files.get` をtry/exceptでラップし、通信エラー時に例外画面ではなく日本語エラーを表示。動画長さ超過時のファイル削除を `cleanup_video`（best-effort）に変更し、削除失敗が長さエラーをマスクしないよう修正（Y-3）
- `ADMIN_PASSWORD` 未設定時に空パスワードで管理者ログインできる問題を修正（未設定時は常に拒否）（Y-4）
- screenerの空レスポンス（`response.text` がNone）や「```」だけの応答で生エラーが表示される問題を修正。JSONパース失敗と同じ「通過（診断優先）」フォールバックに統一（Y-5）

#### フェーズ3（任意）
- screenerに503ハンドリングを追加（analyzerと対称に）（G-1）
- 動画処理タイムアウト時にFiles API上のファイル削除を試みるよう変更（48hの自動削除への依存を解消）（G-2）
- `.streamlit/config.toml` に `[server] maxUploadSize = 200` を明示（`MAX_VIDEO_SIZE_MB` との一致を固定）（G-3）
- 管理者パスワード比較を `hmac.compare_digest` に変更（タイミング攻撃対策）（G-4）

---

## v1.5.1 — 2026-06-11

### 🤖 Gemini 3.5 Flash パラメータ最新化
- 診断モデル（analyzer）は既に `gemini-3.5-flash`。思考パラメータを `thinking_budget`（数値）から推奨の `thinking_level`（段階）へ移行。深いバイオメカニクス推論のため `"high"` を指定。
- `temperature` を screener から削除。Gemini 3.x ではサンプリングパラメータ（temperature/top_p/top_k）が非推奨化され、デフォルト設定が最適化済みのため（公式ドキュメント準拠）。
- `requirements.txt` の `google-genai` を `>=2.7.0` に引き上げ（`thinking_level` 対応バージョンを保証）。

---

## v1.5.0 — 2026-06-03

### 追加

- 診断結果の直下に、補強メニュー実践用の筋トレ・フィットネスグッズへ誘導するCTAカード（`render_gear_cta()`）を追加。AmazonおすすめリストへのリンクをCTRの高い「結果直後・ダウンロードボタンの前」に配置
- リンク先URLを `config.py` の `AMAZON_FITNESS_LIST_URL` で管理（アフィリンクのため `rel="noopener noreferrer sponsored"` を付与）
- 収益動線が無かった本アプリにアフィリエイト導線を新設（Amazonストアの「ランナーの補強・筋力トレーニング」リストへ送客）

---

## v1.4.0 — 2026-06-03

### 変更

- 診断結果のセクション3「具体的なトレーニング提案」で、提案範囲をドリルだけでなく筋力トレーニング・モビリティ・プライオメトリクス・コア等を含む種目全体に拡張（SDTのフォーム診断プロンプトと文言を統一）
- 各提案に「実施タイミング（ウォームアップ後・本練習後・別日など）」を追記するよう変更
- 距離ランナーのフォーム不良（殿筋・体幹の筋力不足、弾性不足が原因のもの）に対し、ドリル以外の補強も処方できるようにすることが目的（コーチングの過小処方を解消）

---

## v1.3.2 — 2026-05-24

### 修正

- 14秒など明らかに5秒以上の動画が「5秒未満のため診断できない」と誤判定されるバグを修正
- 動画の長さチェックをGeminiスクリーナーから切り離し、Files APIのメタデータ（`video_metadata.video_duration`）を使ってPython側で正確に判定するよう変更
- スクリーナープロンプトから動画長さのチェック項目を削除（人物・全身確認のみに絞り込み）
- `MIN_VIDEO_DURATION_SEC`・`MAX_VIDEO_DURATION_SEC` を `config.py` に定数として追加

---

## v1.3.1 — 2026-05-21

### 変更

- PC表示でサイドバーをデフォルトで閉じた状態に変更（`initial_sidebar_state="collapsed"`）

---

## v1.3.0 — 2026-05-21

### 変更

- メイン診断モデルを `gemini-3.1-pro-preview` から `gemini-3.5-flash` に移行
- Gemini 3.5 Flash の仕様に合わせてAPIパラメータを更新
  - `temperature` / `top_p` / `top_k` を削除（3.5 Flash はデフォルト設定に最適化済みのため非推奨）
  - `thinking_budget` を 8192 → 16384 に増加（3.5 Flash の上限拡大を活用）

---

## v1.2.3 — 2026-05-12

### 修正

- iframeに埋め込んだ場合にcookie制限が無効化される問題を修正
- cookie書き込み時に `SameSite=None; Secure; Partitioned` を設定（Chrome 114+ / Firefox対応）
- `expires` を明示的に2日後に設定（日付またぎ後も確実に残るよう）
- ※ Safari は ITP によりiframe内ストレージ自体がブロックされるため回避不可

---

## v1.2.2 — 2026-05-12

### 修正

- cookieが書き込まれないバグを修正（リロードで制限がリセットされる問題）
- 診断完了後に `st.rerun()` を呼ぶことで、次の描画サイクル先頭の `cookie_write_pending` ブロックが確実に実行されるよう変更
- 診断結果をセッション状態（`last_result`、`last_context`）に保存して rerun 後に表示

---

## v1.2.1 — 2026-05-12

### 変更

- 1日あたりの診断回数上限を3回→1回に変更

---

## v1.2.0 — 2026-05-12

### 変更

- 使用回数制限をセッションベース（ブラウザ再読み込みでリセット）からcookieベース（1日3回）に変更
- `streamlit-cookies-controller` を追加（`requirements.txt`）
- 制限超過時のメッセージを「明日またお試しください」に変更
- cookieキー：`rfd_date`、`rfd_diag_count`

---

## v1.1.1 — 2026-05-09

### 修正

- ダウンロードしたMarkdownファイルをスマホで開くと文字化けする問題を修正（エンコードを `utf-8` → `utf-8-sig` に変更）

---

## v1.1.0 — 2026-05-08

### 追加・変更

- 管理者モード：サイドバーパスワード認証による診断回数制限のバイパス機能
- 1セッションあたりの診断回数制限（3回）を追加
- アプリタイトルをバージョン番号つき表示に変更（`v{APP_VERSION}`）
- コンテキスト入力欄の高さ調整・例文を具体的なインターバル練習・レース動画の例に更新
- スクリーニング判定を複数選手のレース動画（100m走など）に対応するよう緩和
- フッターのMPC・MTPリンクURLを修正（`/lp/marathon-simulator/`・`/lp/ai-marathon-coach/`）

---

## v1.0.0 — 2026-05-08

### 初回リリース

- 動画アップロードによるランニングフォーム診断機能
- gemini-3.1-flash-lite による動画スクリーニング（画角・人物確認）
- gemini-3.1-pro-preview + Thinking Budget 8192 tokens による深層診断
  - 全体評価（良い点）
  - 改善すべき点（力学的根拠つき）
  - ドリル・トレーニング提案
- 練習内容・気になる点のコンテキスト入力欄
- 診断結果のMarkdownファイルダウンロード（診断日・コンテキスト入りシグネチャ）
- ダークテーマUI（AkiRun赤系 #FF4B4B）
- Streamlit Community Cloud へのデプロイ対応
